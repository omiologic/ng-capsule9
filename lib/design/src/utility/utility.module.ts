import { NgModule } from '@angular/core';
import { ResizeService } from './resize.service';

/*
 * All utility tools developer needs for UI
 */
@NgModule({
  providers: [
    ResizeService
  ]
})
export class DesignUtilities { }
